#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
    ll N, M;
    cin >> N >> M;
    cout << string(200, '9') + string(199, '0') + string(1, '1') << '\n';
    cout << string(200, '9');
    return 0;
}